/*window.addEventListener('scroll', function(e) {
	for (var i = 0; i < each_article.length; i++) {
		var content = each_article[i].querySelector('.content');

		if (content.style.transform < 58) {
			content.style.transform = 'translateY(-' + (window.pageYOffset / 2) + 'px)';
		}
	}
}, false);*/